# data sprint — remainder

What's left from the data sprint. This document retires when the list is empty.

## Up next

- **xray**, related to the security of the database — the user will explain the scope. The stub exists as xray.js, sketched as an out-of-band system to confirm what gets built into bundles, a capstone to the secrets-management context.

## Small items

- Prune level2's `pgliteDynamicImport` import, unused there since the grid.js move.
- Give the newer documents their contents.md lines, pending a possible rename or combine.
- jsonb.md now holds only the efficiency guidance for future json columns; fold it into ledger.md beside the k1–k8 plan and retire the file, or keep it standalone.
