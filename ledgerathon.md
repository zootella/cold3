# ledgerathon

The plan for the sprint that has every mutation of a live table write a ledger row beside it, from the same function that mutates, so nothing that happens here goes unrecorded: every credential flow, sign-up and close account, and beyond our own tables the dealings with third parties no record covers. It is the first of three sprints in a fixed order, planned in data.md: the ledgerathon, then hideless, which retires the hide column and converts every table to editing cells and deleting rows, then the smaller dog, which removes the Datadog apparatus. The order runs backward from the reason. Datadog can only leave once every change in the system has its record in ledger_table, and those records are only proven complete once the tables have stopped keeping history of their own, because a fact a mutating table loses is lost only to that table and never to the ledger. So this sprint runs first and soaks, for at least a week of real use, before hideless deletes anything.

The rule it installs is permanent. From the ledgerathon on, a new mutation arrives with its ledger row, in the same turn that writes it, and a review that finds a mutation without one has found a bug. It is a sprint of many sessions, and a new session takes it, reading this document to start. Nothing in it is built.

## The three kinds of record

The ledger documents three kinds of thing completely, and the inventory below is sorted by them without saying so.

- **Changes of state inside the application.** The create, update, and delete rows this document is mostly about: Alice changes her password, removes an email, adds a phone. Kept so a later version of the site can act on the history, reminding a user whose password is a year old, or showing staff what an account went through.
- **Complete records of every interaction with a third party.** We told Twilio this and they answered this, with the tick on each row, so a query can show that a send always took five seconds until August 7 and fifteen since. The round robin system reads these rows to alert staff to a provider misbehaving, and a robin running inside the site could balance away from a misbehaving provider by itself while staff sleep.
- **Records around addresses that could be contested between users.** Phone numbers, email addresses, wallet addresses, and oauth accounts: two users each believing they are alice.k@gmail.com, or someone scanning by misusing one of the credential flows. These rows are the radar that shows where the system is being pushed, kept running always so a vulnerability is understood from its record long after.

## What the ledger holds today

Verified against the hosted database and the code on September 7, 2026. ledger_table holds 2,910 rows, and 2,901 of them are hits, one per browser per hour. The rest are the three production call sites: the otp send in level3, whose row carries the whole task the lambda returned, and the two sites in the oauth handler, the proof or refusal at the callback and the cancel. Two rows still carry MessageSent., the action the send wrote before the vocabulary settled, and they stay as they are: the ledger is a notebook, an old row keeps the shape the code of its day gave it, and wrapper_hash points at that code. Nothing in production reads the table.

The shape is general on purpose, and this sprint asks nothing new of it. The margins, then wrapper_hash for the build that wrote the row; ip_text, origin_text, and client_json's geography and browser, filled from the door on every row; browser_hash and user_tag_text for who was here; action_text, event_text, and provider_text for what happened, in tags rather than codes; hash_text for the one thing the row is about, so every record about an address is one indexed lookup; and json for everything else. Six indexes, by browser, user, action, hash, event, and provider, newest first. _ledgerRow in level3 is the one place a row is assembled, and it reads the door through getDoor, which tosses when there is none, because a ledger row with no request behind it is a bug in the caller.

## The rules

- **Every mutation of a live table writes its ledger row, and the function that mutates is the function that records.** Not the endpoint above it, so a new caller cannot forget, and a grid test that walks the function proves the row.
- **A ledger row says who acted, on what, from where, and when.** user_tag_text is the user who was authenticated to make the request. browser_hash, ip_text, origin_text, and client_json come from the door. row_tick is the moment. wrapper_hash is the build. hash_text names the thing acted on when there is one, and json holds the rest.
- **A ledger row records what the function was asked to do, from the values it was given.** Never what the table held before the change or after it. The table change and the ledger write read the same local variables, and neither reads the other's result, so the two calls stay independent and the row can be written from the parameters alone. History is the rows read in order: the sessions a sign-out ended are the sign-ins above it, and the name a change replaced is the earlier Name. Proven. row.
- **A record table is never mutated, and a record gets no ledger row.** ledger_table, delay_table, and trail_table are appended to, and a wrong record is corrected by a later record.
- **A dealing with a third party is its own row, separate from the row about the user's data.** The otp send writes one row about Twilio, what we asked, what they answered, how long it took, and a second row that Alice's address is challenged; wallet prove step 2 writes one row about Alchemy and a second that the address is proven. A robin query by provider finds the first kind without wading through the second, and a staff query by user finds the second without the provider's payload in the way.
- **An old row is never migrated to the current shape.** The ledger is a notebook: a row keeps the shape the code of its day gave it, wrapper_hash names that code, and a reader who meets an old shape looks the build up rather than expecting the table to speak one language.
- **The ledger is the history.** A live table's own history, the hidden rows credential_table keeps today, is a duplicate with less in it, and hideless removes it once this sprint has made the ledger complete.

## The convention

Every mutating function in level3 has the same three parts, top to bottom: the checks on its parameters, the table change, and the ledger write, on adjacent lines, both reading the same locals. The ledger write comes after the table change succeeds. The endpoint passes nothing new, once the door carries the browser hash the way it carries the ip, origin, geography, and agent that _ledgerRow reads today. doorWorker and doorWorkerLite compute the hash from the cookie and hand it down as a parameter, and nothing pins it on the door, so a mutation deep in level3, credentialNameSet with its one userTag parameter, has no browser hash in hand. The sprint pins it as door.browserHash at the two doors that have one, _ledgerRow reads it from there by default, and the grid door gains one for the tests. The alternative, threading a browserHash parameter through every mutating signature, is a decision below. credentialOtpSend, which takes a browserHash parameter today only for its ledger row, drops it, so every function reads the hash the same way. The two oauth sites that write ledger rows from site code today move down: the proof row into credentialOauthSet, which already receives the provider's proof, and the refusal row beside it. The Cancelled. row stays in the handler, because a cancel at the provider mutates nothing and no level3 function runs; when hideless has a cancel delete the challenge row through a level3 call, the row moves down with it. Decided September 7, 2026.

ledgerAdd tosses without a door. Every mutation in production runs below one of the three doors, and every grid test runs below the grid door, so this costs nothing new. A one-off script that mutates rows would run its work inside doorAsyncLocalStorageRun with a small script door, which is a new and tiny thing to write the first time a script needs it.

## The order of the work

A small first pass, then the long one. The first pass is the door pin and the oauth move: door.browserHash on the two worker doors and the grid door, _ledgerRow reading it by default, credentialOtpSend's browserHash parameter gone from the function, the endpoint, and the tests, and the oauth proof row moved from the handler into credentialOauthSet. Every row after depends on the pin, and the move proves the shape of a ledger write inside a level3 function on a site that already has one. The long pass is the inventory below, type by type, each function gaining its ledger write beside its table change and its grid test asserting the rows; the two checks the endpoint does inline today, the password compare at sign-in and the totp code check, move into level3 in that pass so their rows are written beside them. The upload lambda's rows are their own turn near the end, since they touch net23's door, build, and deploy.

## The vocabulary

action_text is the subject, which for credential_table is the credential type, Browser., Name., Password., Totp., Ethereum., Oauth., Email., or Phone., the same word the credential row carries in type_text, so a ledger query by action and a credential query by type speak the same names. event_text is the verb. Four are in use today: Challenged., Proven., Refused., and Cancelled. The rest arrive as the inventory below is coded, chosen at each site by two habits: the same word when it is the same thing, and the word the surrounding flow and its table already use, so a sign-in is Browser. Proven. because the user proved control of the browser, and a sign-out is Browser. Removed. because remove is what the function is called. The list is not an enumeration to complete or enforce: checkActionOrBlank checks the shape of a tag and not a list, a comment beside ledgerAdd lists the words in use, and the grid tests pin the exact word each flow writes.

## What json carries, and never carries

The facts that make the row useful later, that no column holds, and that the function was given: the tag of a challenge, the address forms, the connector a wallet came in over, the cycles of a password, the identifier and handle of an oauth link, the outcome of a refusal. Never a secret: no password hash, no totp secret, no wallet nonce still live. The one exception already decided stays decided, the otp send row holds the lambda's whole task with the message text, per data-cleanup.md's closed item, because the row is the complete record of what we did with the provider.

## The inventory

Every function that mutates, and the row it writes. The credential rows a flow writes today are unchanged by this sprint, and hideless.md describes what they become; this list is only what the ledger gains.

**Browser.** credentialBrowserSet writes Browser. Proven. with hash_text the browser's hash, so the session is findable by browser as well as by user. credentialBrowserRemove writes one Browser. Removed. for the user, with no hash, since it ends every session at once and is given only the user; the sessions it ended are the Browser. Proven. rows above it in the user's history.

**Name.** credentialNameSet writes Name. Proven. with the three new forms; the name it replaced, if any, is the earlier Name. Proven. row. credentialNameRemove writes Name. Removed. with nothing but the user, since remove is given only the user. A refused set, name taken or invalid, writes nothing: names are public, so a taken name is no signal and an invalid one is a typo. No hash rides a Name. row; the normalized name is already in json, and a hash of it would duplicate it for no query anyone runs.

**Password.** credentialPasswordSet writes Password. Proven. with the cycles. credentialPasswordRemove writes Password. Removed. No hash rides the ledger.

**Totp.** credentialTotpEnroll1 writes Totp. Challenged. credentialTotpEnroll2 writes Totp. Proven. on success and nothing on a wrong first code, since the secret is on the user's own screen and nobody else is involved. credentialTotpClear writes Totp. Cancelled. credentialTotpRemove writes Totp. Removed. The code check at sign-in, which the endpoint does inline today under TotpValidate., moves into a small level3 function that writes Totp. Proven. on a right code, Totp. Refused. with Wrong. on a wrong one, and Totp. Refused. with Later. when the guard trips on too many wrong codes inside the horizon: an attacker here has the password already and is at the inner door, and the trail counts the guesses while the ledger names the ip of each.

**Ethereum.** credentialWalletProve1 writes Ethereum. Mentioned. with hash_text the hash of the address, the mention's home, and its only one once hideless removes the credential row, then Ethereum. Challenged. with the nonce and connector, or Ethereum. Refused. with the outcome when the refusal check declines, WalletClaimedElsewhere. being the contested one and WalletAlreadyProven. and WalletFull. riding the same line. credentialWalletProve2 writes Ethereum. Proven. with the nonce that proved it, or Refused. with the outcome: BadSignature. for a message that doesn't verify, Expired. for a nonce we never issued or already spent, Later. when the chain provider didn't answer, or a slot that filled while the user was signing. When step 2 asks the chain, a smart contract wallet, it writes a separate row about that dealing first, provider Alchemy., with what was asked, what came back or the error, and how long it took, so a query by provider shows how Alchemy has been answering. credentialWalletRemove writes Ethereum. Removed.

**Oauth.** credentialOauthChallenge writes Oauth. Challenged. with the provider. credentialOauthSet writes Oauth. Proven. with the provider, identifier, handle, name, and the proof, the row the handler writes today, or Oauth. Refused. for already-linked and claimed-elsewhere. credentialOauthRemove writes Oauth. Removed. The handler keeps Oauth. Cancelled.

**Email. and Phone.** credentialOtpSend writes the type's Mentioned. row on every send, with the outcome in json when the send was refused, Held., CoolSoft., or CoolHard., so the evidence that a confused user keeps typing an address that isn't theirs, or that someone is hammering one address with codes, stands in the ledger, beside the credential row until hideless removes it. The one row the send writes today, Challenged. with the lambda's whole task in json, becomes two: a row about the dealing with the provider, Twilio. or Amazon., carrying the task, the request, the response, the error, and the duration, and a second row that the address is Challenged., with the tag; the rows already written keep their shape. credentialOtpEnter writes the type's Proven. with the tag that proved it. On a wrong guess it writes Refused. with Wrong. and the guesses left: the trail counts the guess, but the ledger names the ip of the attacker who learned Alice's number and is guessing at her code. The fourth wrong guess writes Expired. instead, since it closes the challenge. Held., when another user proved the address while this challenge was live, writes Refused. with Held. and closes the challenge. A guess against a challenge that isn't live, a stale tab after the twenty minutes, writes nothing; abandoned challenges show as a Challenged. row with no end. credentialOtpRemove writes one Removed. row with the hash of the address.

**Close account.** credentialCloseAccount writes one Account. Closed. row for the user. What the account held at closing is the Proven. and Removed. rows above it.

**Sign-up.** The endpoint mints a userTag and calls three set functions, which write their three rows. No Account. Created. row now: signing up by choosing a unique name is a temporary bootstrap that stands in for account creation until that flow is designed, and the rows about an account coming into being arrive with that design rather than being pinned to this on-ramp.

**Password sign-in.** The endpoint looks up the name, fetches the password row, and compares the hash inline today, answering InvalidCredentials. on any miss. That compare moves into a small level3 function, which writes Password. Refused. with the name asked for on a miss, so a run of misses against one name, credential stuffing, stands in the ledger with the ip of each try, and on a hit the sign-in's Browser. Proven. row comes from credentialBrowserSet as it does everywhere. The cycles lookup the page makes before sign-in writes nothing: names are public, and the miss is recorded at the sign-in itself.

**settings_table.** Excluded, like example_table: it holds the demo counter and nothing about a user, so settingWrite and settingRead write no ledger row and the counter demo stays as it is. Decided September 7, 2026.

**Beyond our own tables.** The ledger's second kind of record is dealings with third parties, and data-cleanup.md lists three that no record covers: the turnstile failure, the upload lambda's storage calls, and the chain provider call in wallet prove step 2. The chain call gets its row as described under Ethereum. above. The upload lambda writes its own rows, from the lambda: it is real Node, it imports icarus like the worker does, icarus depends on supabase-js so the bundle carries the client, and the wrapper seals every secret for every server bundle, so the lambda decrypts the database key the way it decrypts the rest and no secret gains a new home. Three pieces make it work. doorLambdaOpen fills the four client cells a ledger row reads from a door, the ip from the function url's request context, the origin and the agent from the headers, and geography {} since no Cloudflare stands in front; the permission envelope the worker seals for an upload carries the browserHash and the userTag, so the lambda's rows name who was here from the worker's sealed word rather than the page's; and the rows are a summary rather than a flurry, one when S3 creates the upload and one when it completes or aborts, none per part. The rows are there partly as a reminder: the upload code will be worked on again, and a few ledger writes in it say the pattern applies here too. Turnstile is a special case and gets no ledger row. A failed bot check is not a change to our data; it is what logAudit was for, a record of our experience with a third-party service, so that trouble at four in the morning can be worked out later. Its check also tosses inside level2 before any endpoint runs, and level2 cannot import ledgerAdd from level3, a seam not worth dictating the layering of the application. The smaller dog picks its channel: console.error to the Cloudflare dashboard, or the one async dog that survives, to Datadog. The lambda's own audit of the message task is not converted, since the worker already writes that row, and it leaves with the smaller dog.

**Records.** recordHit, recordDelay, and the trail writers write records, not mutations, and get no ledger row.

## The two writes

PostgREST has no transactions, so a table change and its ledger row are two round trips, one after the other. The order is the table change first and the ledger write second, so a ledger row never says something happened that didn't. Nothing special handles a failure. A failed call throws, the door answers the request with a 500, and the error lands in the provider dashboard. If the table change failed, nothing changed and no row was written. If the ledger write failed, the change stood and its row is missing, which is the gap hideless makes visible once rows are deleted rather than hidden. Server code can't be written to hold a predictable state through an unreliable database, so it doesn't try: no retry, no compensating write, no try/catch around either call. The two calls run in sequence rather than at once, because the comment at the bottom of report.js records that firing two Supabase calls at once from one request caused delays and 409 errors. If a missing row ever matters in practice, a Postgres function called through supabase.rpc runs inside one transaction and is the one path in the stack that could make the pair atomic, recorded here so the day it is needed nobody rediscovers it.

## The cost

An awaited ledger write is on the order of a hundred milliseconds on every path that mutates. Sign-in is the hottest of them and gains one write, on a path that already makes eleven round trips, and sign-ins are rare here: the browser tag cookie lives 395 days from the last visit, where other sites sign a user out after thirty days or an ip change. Accept it plainly: the paths are rare and human-paced, a person just typed a code or signed a message, and the alternative, a fire-and-forget successor to keepPromise, is exactly the parking lot the smaller dog is removing. Sign-up calls four functions and gets four rows in four calls, one from each, which is the price of the rule that the function that mutates is the function that records; sign-up is rare. ledgerAddMany stays for the day one function has several rows to write.

## Testing

Every grid test that walks a flow asserts the ledger rows beside the credential rows: how many, which action and event, which hash, and the facts in json. Nothing in production reads ledger_table, and the tests need no reader of their own: the ledger tests already read rows with queryGet on ledger_table, filtered by action, user, or hash, and the flow tests read the same way. A ledgerGet family waits for the first staff page or robin query that wants one. A rule a test proves in isolation is worth little, so the assertions ride the flow tests, not a separate suite.

## Decisions the sprint must make

Each with a recommendation; the ones marked decided were taken September 7, 2026, and the rest are open.

- **Where a mutation gets its browser hash.** Pinned on the door as door.browserHash by the two doors that compute one, and read by _ledgerRow by default, so no level3 signature changes, rather than threaded through every mutating function, which is the shape smaller-dog.md sketches for turnstile alone. credentialOtpSend drops the browserHash parameter it carries today only for its ledger row. Decided September 7, 2026.
- **The order of the two writes and the failure.** Table change first, ledger write second, in sequence, a failed call tosses to the top gate, and nothing else. Decided September 7, 2026, with the rpc escape hatch recorded.
- **The verb list.** Not an enumeration to complete or enforce. The same word when it is the same thing, and the word the surrounding flow and its table already use, chosen at each site as the inventory is coded, with a comment beside ledgerAdd listing the words in use. Decided September 7, 2026.
- **Whether refusals get rows.** Decided September 7, 2026, by two tests, either of which says yes: the refusal sits in a function whose other paths pair a table change with a ledger row, so leaving it out would be the one conspicuous gap in a painted fence; or the refusal is about, near, or could help tell the complete picture of an attack or malice. So every refusal a credential function returns gets its row, including the user's own harmless ones like WalletAlreadyProven. and OauthAlreadyLinked., and so does every wrong otp or totp guess, because the trail counts a guess but the ledger names the ip that made it. The exceptions are the paths where nobody else is involved and nothing is contested: a wrong first code during totp enrollment, a name that is taken or invalid, since names are public, the cycles lookup before sign-in, and a guess against a challenge that isn't live, the stale tab, which is counted as a start with no end rather than by rows of its own.
- **Whether sign-up writes an Account. Created. row, and where.** Not now. Decided September 7, 2026: the name sign-up is a temporary bootstrap, and account creation gets its ledger rows when account creation is designed.
- **Whether Name. rows carry a hash_text.** No. Decided September 7, 2026: the normalized name already rides json, and a hash of it would duplicate the data for no query anyone runs.

## What this document leaves alone

hideless, which converts the tables and is where the credential rows this sprint writes beside become one row per credential; nothing about how credential_table hides and ranks changes here. Turnstile's channel, which the smaller dog picks. The smaller dog's removal pass, which takes logAudit, the lambda's own audit of the message task, and the parking lot once this sprint and hideless have landed. Robin, which reads what this sprint writes and is planned wherever robin is planned.
