{
	"name": "net23",
	"description": "Network 23",
	"license": "GPL-3.0-only",
	"version": "0.1.0",
	"type": "module",
	"private": true,
	"repository": {
		"type": "git",
		"url": "https://github.com/zootella/cold3.git"
	},
	"scripts": {
		"local": "serverless offline --reloadHandler",
		"build": "node build.js && cd dist && serverless package && cd .. && node size.js && node ../test.js",
		"cloud": "node build.js && cd dist && serverless deploy && cd .. && node size.js && node ../test.js",
		"deploy-only": "cd dist && serverless deploy && cd .. && node ../test.js",
		"www": "aws s3 sync ./www s3://www-net23-cc --delete"
	},
	"dependencies": {
		"@sendgrid/mail": "^8.1.6",
		"icarus": "workspace:*",
		"sharp": "^0.34.5",
		"twilio": "^5.12.1"
	},
	"devDependencies": {
		"@aws-sdk/client-s3": "^3.985.0",
		"@aws-sdk/client-ses": "^3.985.0",
		"@aws-sdk/client-sns": "^3.985.0",
		"@aws-sdk/s3-request-presigner": "^3.985.0",
		"@vercel/nft": "^0.29.4",
		"fs-extra": "^11.3.3",
		"serverless": "^4.31.2",
		"serverless-offline": "^14.4.0"
	}
}
