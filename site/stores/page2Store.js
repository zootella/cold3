//./stores/page2.js - additional page store as a temporary landing place for web3 client side stuff

export const usePage2Store = defineStore('page2Store', () => {

//example
const helloMessage = ref('starting hello message')

return {
	helloMessage,
}

})
